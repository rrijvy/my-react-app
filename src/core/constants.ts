const HttpHeaders = {
  ContentType: {
    ApplicationJson: {
      "Content-Type": "application/json",
    },
  },
};

const DateFromats = {
  default: "MM-DD-YYYY",
};

export const AppConstants = {
  HttpHeaders,
  DateFromats,
};
